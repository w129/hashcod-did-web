{
  "name": "hashcod-did-web",
  "version": "1.0.0",
  "private": true,
  "description": "did:web public-only HASHCOD publisher (Render + local)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "node -e \"console.log('no build step — static + API server')\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}
